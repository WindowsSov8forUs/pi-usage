{
  "name": "pi-usage",
  "version": "0.1.1",
  "description": "Show provider quota, balance, budget, and session usage in Pi.",
  "private": true,
  "type": "module",
  "license": "MIT",
  "keywords": [
    "pi-package",
    "pi-extension",
    "usage"
  ],
  "scripts": {
    "test": "node --import ./test/register-pi.mjs --experimental-strip-types test.mjs"
  },
  "peerDependencies": {
    "@earendil-works/pi-ai": "*",
    "@earendil-works/pi-coding-agent": "*",
    "@earendil-works/pi-tui": "*"
  },
  "pi": {
    "extensions": [
      "./index.ts"
    ]
  }
}
